import React from 'react';
import ReactDOM from 'react-dom/client';
import JudgeAppWithLogic from './VBARCalculator';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <JudgeAppWithLogic />
  </React.StrictMode>
);